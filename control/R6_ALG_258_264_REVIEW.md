# ALGEBRA-258–264 independent canon review

The seven rows were replayed against the exact pinned `algebra.tex` authority
at SHA-256
`FA8BB92E58A4F78A2BD01B3B6A4A87DE0A0D279F5DD90641B574DD5FBFFFA4F3`.
All seven are novel at their exact loci.

- ALGEBRA-258 is a typed notation omission: the fibre ring is
  `S \otimes_R \kappa(\mathfrak p)`, as line 15630 and the later equality at
  line 15670 both show.
- ALGEBRA-259 omits the already defined and repeatedly used `A'_{fin}` from
  the comparison lemma's inventory.
- ALGEBRA-260 is the certain agreement correction `prove` to `proves`.
- ALGEBRA-261 must refer to the quotient module `N/\mathfrak p'N` established
  two lines earlier.
- ALGEBRA-262 must use `Ass_{S_{\mathfrak q}}`; the prime
  `\mathfrak qS_{\mathfrak q}` and the localized module both belong to the
  localized ring, exactly as line 15809 records.
- ALGEBRA-263 inserts `an` at both and only the two exact occurrences.
- ALGEBRA-264 supplies finite verbs to the two coordinated clauses.

No authority or translation byte was changed. All seven corrections are held
as non-rendering pointers for the independently replayed English AI derived
fork.
